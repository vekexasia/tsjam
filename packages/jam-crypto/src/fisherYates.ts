import { Hash } from "@vekexasia/jam-types";

export const FisherYates = <T>(arr: T[], entropies: number[]) => {
  return arr.slice();

  // todo : implement
  // the graypaper has a very inefficient formalism for fisher yates
};
export const FisherYatesH = <T>(arr: T[], entropy: Hash) => {
  return arr.slice();
};
