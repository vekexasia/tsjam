{
  "name": "@tsjam/pvm-wasm",
  "version": "0.7.1",
  "description": "pvm wasm",
  "scripts": {
    "dev": "$npm_execpath run build -w",
    "lint": "npx eslint --ext .ts assembly",
    "test": "vitest",
    "coverage": "vitest run --coverage",
    "prepublishOnly": "npm run build && vitest run",
    "build": "npx ts-macros transform out --tsconfig tsconfig.asm.json && rm -rf assembly && mv out/asm assembly && rm -rf out && yarn lint assembly --fix && cp asm/tsconfig.json assembly/ && yarn asbuild && echo done",
    "asbuild:debug": "asc assembly/index.ts --target debug",
    "asbuild:release": "asc assembly/index.ts --target release",
    "asbuild": "echo ciao && yarn asbuild:release",
    "start": "npx serve ."
  },
  "exports": {
    ".": {
      "import": "./dist/index.js",
      "types": "./dist/index.d.ts"
    }
  },
  "engines": {
    "node": ">= 22.0.0"
  },
  "author": {
    "url": "https://www.andreabaccega.com/",
    "email": "vekexasia@gmail.com",
    "name": "Andrea Baccega"
  },
  "dependencies": {
    "@tsjam/pvm-base": "*",
    "@tsjam/types": "*",
    "as-bigint": "^0.5.3",
    "as-console": "^7.0.0"
  },
  "license": "MIT",
  "devDependencies": {
    "@types/json-bigint": "^1.0.4",
    "assemblyscript": "^0.28.8",
    "json-bigint": "^1.0.0",
    "ts-macros": "^2.6.2"
  },
  "jam:protocolVersion": "0.7.1",
  "type": "module"
}
